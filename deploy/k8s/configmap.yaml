apiVersion: v1
kind: ConfigMap
metadata:
  name: doorbell-config
data:
  config.yaml: |
    server:
      host: "0.0.0.0"
      port: 8080
    hikvision:
      host: "192.168.1.100"
      username: "admin"
      password: "your-password"
